name: Smoke test

on:
  push:
    branches: [main]
  pull_request:
  schedule:
    - cron: '0 4 * * 6' # Every Saturday at 4:00 UTC
  workflow_dispatch:

jobs:
  smoke:
    uses: ./.github/workflows/smoke-run.yml
